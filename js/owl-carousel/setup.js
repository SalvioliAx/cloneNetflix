$('.owl-carousel').owlCarousel({
    loop:true,
    margin:10,
    nav:false,
    responsive:{
        0:{
            items:1
        },
        600:{
            items:3
        },
        1000:{
            items:5
        }
    }
})  
// Get the videoPlayer

var link = document.querySelector('.myModalVideo');// get iframe class 
var link = document.querySelector('#myModalVideo');// get iframe id
var video_src = link.getAttribute('src');

// Get the modal
var modal = document.getElementById("myModal");

// Get the button that opens the modal
var btn = document.getElementById("myBtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks on the button, open the modal
btn.onclick = function() {
  modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
  modal.style.display = "none";
  $('.modal-content').children('iframe').attr('src', ''); // set iframe parent div value null 
  $('.modal-content').children('iframe').attr('src', video_src);// set iframe src again it works perfect
 
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}
